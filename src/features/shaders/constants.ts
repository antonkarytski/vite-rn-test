export enum ShadersDefaultVarNames {
  U_RESOLUTION = 'u_resolution',
  U_TIME = 'u_time',
  U_MOUSE = 'u_mouse',
  A_POSITION = 'a_position',
}

export enum WebGLVarType {
  FLOAT = 5126,
}

export enum DrawType {
  STATIC_DRAW = 35044,
  DYNAMIC_DRAW = 35048,
}
